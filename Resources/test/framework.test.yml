parameters:
    locale: "en"
    secret: "efervescencio"

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    secret: "%secret%"
    form: false
    default_locale: "%locale%"
    templating: false
    assets: false