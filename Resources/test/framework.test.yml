parameters:
    locale: "en"
    secret: "efervescencio"

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    secret: "%secret%"
    router:
        resource: ~
    form: true
    default_locale: "%locale%"
    templating:
        engines: ['php'] }